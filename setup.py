from setuptools import find_packages, setup

setup(
    name="llm-from-scratch",
    version="0.1.0",
    description="A fully manual GPT-style language model built from scratch with PyTorch",
    packages=find_packages(),
    include_package_data=True,
)
